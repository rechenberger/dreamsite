mutation SaveBlocks($blocks: [cms_block_insert_input!]!) {
  insert_cms_block(
    objects: $blocks
    on_conflict: {
      constraint: block_pkey
      update_columns: [config, position, routeSlug, type, locale, stage]
    }
  ) {
    affected_rows
  }
}
